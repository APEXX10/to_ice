
# จดหมายพิเศษ (Envelope proposal)

ไฟล์ในโฟลเดอร์นี้:

- `index.html` — หน้าเว็บหลัก แสดงซองจดหมายที่เปิดได้
- `styles.css` — สไตล์และอนิเมชันของซองและกระดาษ
- `script.js` — พฤติกรรม: เปิดซองและแสดง confetti

วิธีใช้งาน (Windows):

1. เปิดไฟล์ `d:\iceland\index.html` ด้วยเบราว์เซอร์ (ดับเบิลคลิกหรือคลิกขวา → Open with)
2. คลิกที่ซองจดหมายเพื่อเปิดและดูข้อความ

ปรับแต่งข้อความ:

- เปิด `d:\iceland\script.js` แล้วแก้ค่าตัวแปร `recipient` และ `message` ที่ส่วนบนของไฟล์

เพิ่มฟีเจอร์เพิ่มเติม (ไอเดีย):

- เพิ่มเพลงประกอบเมื่อซองเปิด: ใส่ `<audio>` ใน `index.html` และเล่นด้วย `script.js` เมื่อเปิด
- อ่านค่า `recipient` และ `message` จาก query string เพื่อส่งลิงก์ที่ปรับแต่งได้ (เช่น `index.html?name=นา&msg=...`)
- ทำให้กระดาษเลื่อนขึ้นมาเต็มจอพร้อมปุ่มยืนยัน/ปฏิเสธ

ขอให้สมหวังนะครับ/ค่ะ 🎉
